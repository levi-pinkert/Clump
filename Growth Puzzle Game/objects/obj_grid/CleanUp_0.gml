/// @description Insert description here
// You can write your code in this editor

if(surface_exists(blockSurface)){
	surface_free(blockSurface);
}

if(surface_exists(tempSurface)){
	surface_free(tempSurface);
}

if(surface_exists(wallSurface)){
	surface_free(wallSurface);
}


