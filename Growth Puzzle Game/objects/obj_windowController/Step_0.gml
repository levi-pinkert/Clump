/// @description Insert description here
// You can write your code in this editor

if (browser_width != width || browser_height != height || true)
{
    width = browser_width;
    height = browser_height;
    scaleCanvas(base_width, base_height, width, height, true);
}

